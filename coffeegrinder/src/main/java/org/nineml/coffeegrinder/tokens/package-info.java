/**
 * The token classes.
 *
 * <p>Tokens are what the parser consumes. Often this is a sequence of characters, but it can also be
 * a sequence of strings or, in fact, arbitrary items.</p>
 */
package org.nineml.coffeegrinder.tokens;